# 📋 要件定義ガイド（Claude内部参照用）

> このファイルは `/web-studio:setup` の Stage 3 で使用する内部ガイドです。
> Stage 1・2 の内容は `context.md` に書き出し済みのため、ここでは仕様・表現に関する項目のみ扱う。
> 成果物は `{project}/docs/requirements.md` に書き出します。

---

## Stage 3 ヒアリング項目

`[必須]` 項目はすべてユーザーに確認してから記載する。
`[推測可]` 項目はAIが補完し、未確認事項に列挙する。
`[任意]` 項目は決まっていれば聞く。未定の場合はディレクターが提案し、要件定義書に「提案」として記載する。

**ターゲット・CTA**
- `[必須]` 主要ターゲット: 年齢層・職業・ライフスタイルなど
- `[推測可]` ユーザーがサイトに来るときの状況・気持ち
- `[必須]` 取ってほしい行動（CTA）

**デザイン方向性**
- `[必須]` トーン＆マナー: プロフェッショナル / カジュアル / ミニマル など
- `[推測可]` カラー（指定がなければAIが提案し承認を得る）
- `[推測可]` 参考デザインのキーワードやURL

**コンテンツ・構成**
- `[任意]` 必要なページ・セクション（決まっているものがあれば）
- `[任意]` 入れてほしいコンテンツや機能（決まっているものがあれば）
- `[推測可]` 既存素材の有無（テキスト・画像・ロゴなど）

> 💡 ページ構成が固まっていない場合: Stage 1-2 の内容をもとにディレクターがサイトマップ案を作成し、ユーザーに提案・承認を得る。要件定義書には「提案」であることを明記する。

**技術・運用要件**
- `[推測可]` レスポンシブ: PC / SP / 両方
- `[推測可]` 技術スタック（指定がなければAIが提案）
- `[推測可]` 外部連携: フォーム / 決済 / SNS など

---

## 出力フォーマット

```markdown
# 要件定義書：{サイト名}

作成日: YYYY-MM-DD

> 背景・文脈は `docs/context.md` を参照。

---

## 🔒 ブランドの本質（AIは変更しない）

- **ユーザーに感じてほしいこと:**
- **NGルール:**

### ターゲット
- **主要ユーザー:**
- **ユーザーシナリオ:**
- **CTA:**

### デザイン方向性
- **トーン＆マナー:**
- **カラー:**
- **参考:**

---

## 📐 構成・仕様

### ページ・セクション構成
<!-- 確定 or 提案（ディレクター案） を明記する -->

| ページ / セクション | 内容 | ステータス |
|---|---|---|

### 技術要件
- **スタック:**
- **レスポンシブ:**
- **外部連携:**

---

## ❓ 未確認事項
- （AIが推測で補完した項目、提案中のページ構成などを列挙）
```
